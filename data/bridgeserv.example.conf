/*
 * Example configuration file for BridgeServ.
 */

/*
 * BridgeServ is a pseudoclient, in the same sense as BotServ, HostServ, and the
 * other services. It is through this client that bridges between IRC channels
 * and channels on other networks are managed.
 *
 * A bridge is a mapping between one IRC channel and one channel on a bridged
 * network. BridgeServ itself knows nothing about any particular network: each
 * network is implemented as a bridge protocol, of which Discord is the first.
 * The Discord protocol uses a single bot token and may be present in as many
 * Discord servers (guilds) as you like, of which any guild/channel pair may be
 * bridged to an IRC channel.
 *
 * Every bridged space (a Discord guild, for example) is presented to the
 * network as a virtual link which services carries:
 *
 *   <space-id>.<domain>          eg. 123456789012345678.discord.bridge
 *
 * The users of that space are introduced behind their space's link, and take
 * the following form; note that the space id appears in the link name but
 * deliberately not in the hostmask:
 *
 *   nick     = sanitised remote display name, plus the nickname suffix of the
 *              bridge, if it has one
 *   ident    = hex prefix of the SHA-256 hash of their remote user id, clamped
 *              to networkinfo:userlen
 *   host     = the protocol domain, eg. discord.bridge
 *
 * For example: iampaigeat!a1b2c3d4e5@discord.bridge
 *
 * Every nickname a bridge introduces is reserved on the network for as long as
 * the bridge exists (a Q-line on InspIRCd), so that nobody on IRC can take the
 * identity of somebody on the bridged network, even while that user is not
 * currently present. The reservations are released when the bridge is deleted.
 *
 * Pseudo clients appear when their remote user speaks and are quit again once
 * they have been idle for useridle; their nicknames stay reserved. If two
 * remote users sanitise to the same nickname then a counter is appended
 * (_2, _3, ...) to keep it unique. One client is shared by every bridge with
 * the same space and nickname suffix, so a user active in two bridged channels
 * of one guild is one IRC client in both channels.
 *
 * A bridge may be given a nickname suffix so that the same remote user can be
 * told apart per channel mapping, for example iampaigeat/NC. Suffix characters
 * outside the usual nickname alphabet (such as "/") have to be allowed with
 * networkinfo:nick_chars before they can be used.
 *
 * When messages are relayed from IRC to Discord, the Discord bot uses a
 * webhook per channel so that each IRC user's messages appear under their
 * own nick on Discord. If webhook creation fails (e.g. insufficient
 * permissions), the fallback is the bot account with a "<nick> message"
 * prefix. Messages relayed from IRC never ping anybody on Discord: the
 * allowed mentions of every relayed message are empty, so text like
 * "@everyone" is delivered as plain text.
 */
service
{
	/* The name of the BridgeServ client. */
	nick = "BridgeServ"

	/* The username of the BridgeServ client. */
	user = "services"

	/* The hostname of the BridgeServ client. */
	host = "${services.host}"

	/* The realname of the BridgeServ client. */
	real = "Bridge Service"

	/* The modes this client should use. */
	#modes = "+i"

	/*
	 * The server alias that can be used to securely message this service. If
	 * your IRC server does not have an alias for this service you can set this
	 * to an empty string to tell users to use /msg instead.
	 *
	 * This setting is ignored when options:servicealias is disabled.
	 *
	 * Defaults to the nick of the service if not set.
	 */
	#alias = "BS"
}

/*
 * Core BridgeServ module.
 *
 * Provides the functionality to discover the guilds and channels the Discord
 * bot is present in, and to create, modify, and delete bridges between IRC
 * channels and Discord channels from IRC.
 *
 * This module requires the vendored C++ Discord library (DPP) which is built
 * as part of Anope. DPP is in turn linked against OpenSSL and zlib, which must
 * be available on the build system. If DPP was not built or built without
 * OpenSSL or zlib support this module will fail to load.
 *
 * A single Discord bot token is supplied; the bot account this token belongs
 * to must be invited to each Discord server you want to bridge. The message
 * content intent must be enabled on the Discord application for message
 * content from the bot's guilds to be received, and the bot needs the
 * "Manage Webhooks" permission in each bridged channel for per-user identity.
 *
 * Discord message formatting is converted to IRC formatting (bold, italics,
 * underline, strikethrough, spoilers, and monospace), mentions and custom
 * emoji are rendered as names rather than numeric ids, and attachments,
 * stickers, and replies are annotated. Only message traffic is relayed;
 * joins, parts, presence, reactions, and typing are not.
 *
 * Messages sent by other services on a bridged IRC channel and the webhook
 * echo of this module's own messages are never relayed, to prevent relay
 * loops. Messages from other Discord bots are relayed.
 *
 * Note that the configuration of a bridge, including its webhook token, is
 * stored (unencrypted) in the Anope database. Only allow IRC operators to
 * manage these.
 */
module
{
	name = "bridgeserv"

	/*
	 * The name of the client that should be BridgeServ.
	 *
	 * This directive is optional.
	 */
	client = "BridgeServ"

	/*
	 * The Discord bot token used to connect to Discord. This is the one token
	 * that all bridges share. If this is not set then the Discord link is
	 * disabled and the module only manages its stored bridges.
	 */
	token = "replace-me"

	/*
	 * The bridge protocol which new bridges are created with. Only "discord"
	 * is implemented so far.
	 *
	 * Defaults to "discord" if not set.
	 *
	 * This directive is optional.
	 */
	#protocol = "discord"

	/*
	 * The domain which the virtual links and clients of the Discord protocol
	 * live under: a bridged guild is introduced as the link
	 * "<guild-id>.<domain>" and its users are given "<domain>" as their
	 * hostname. The links are created automatically on the first relay.
	 *
	 * Defaults to "discord.bridge" if not set. Changing this while bridges
	 * are active quits every pseudo client and drops every link.
	 *
	 * This directive is optional.
	 */
	#domain = "discord.bridge"

	/*
	 * The length of time a Discord pseudo client may be idle on IRC before it
	 * is quit. The client is recreated the next time the Discord user speaks.
	 * Set to 0 to keep pseudo clients on IRC forever; any other value below
	 * one minute is raised to one minute.
	 *
	 * Defaults to 1h if not set.
	 *
	 * This directive is optional.
	 */
	#useridle = "1h"

	/*
	 * The maximum number of lines of a single Discord message to relay to
	 * IRC, counted after long lines have been split to fit the IRC line
	 * length. Longer messages are truncated with a notice saying how many
	 * lines were dropped. Must be between 1 and 64.
	 *
	 * Defaults to 8 if not set.
	 *
	 * This directive is optional.
	 */
	#maxlines = 8

	/*
	 * The relay throttle: at most floodlines lines may be relayed from Discord
	 * to an IRC channel in a burst, after which one more line may be relayed
	 * every floodsecs. Lines which do not fit are dropped, and a single notice
	 * reports how many were dropped. Set floodlines to 0 to disable the
	 * throttle entirely (not recommended on a busy guild). floodlines may be
	 * at most 100 and floodsecs must be between 1s and 1h; values outside
	 * those ranges are clamped and logged.
	 *
	 * Default to 6 lines and 4s if not set.
	 *
	 * These directives are optional.
	 */
	#floodlines = 6
	#floodsecs = "4s"

	/*
	 * Whether to relay Discord message edits to IRC. Edits are relayed with
	 * an "(edit)" prefix, and only when the rendered message actually changed.
	 *
	 * Defaults to yes if not set.
	 *
	 * This directive is optional.
	 */
	#relayedits = yes

	/*
	 * Whether to relay Discord message deletions to IRC as a notice. Only
	 * messages which were relayed by this module are reported.
	 *
	 * Defaults to no if not set.
	 *
	 * This directive is optional.
	 */
	#relaydeletes = no

	/*
	 * Whether to relay typing notifications in both directions: a Discord
	 * member typing is shown on IRC as +typing=active from their pseudo
	 * client (once per three seconds at most), and an IRC user's typing
	 * notification triggers Discord's indicator for the channel (once per
	 * eight seconds per bridge). Discord has no per-webhook typing, so the
	 * indicator there shows the bot account's name.
	 *
	 * Defaults to yes if not set.
	 *
	 * This directive is optional.
	 */
	#relaytyping = yes

	/*
	 * Whether to relay reactions in both directions. A Discord reaction is
	 * sent on IRC as a TAGMSG carrying +reply and +draft/react (or
	 * +draft/unreact) from the member's pseudo client; the same tags from an
	 * IRC user become a reaction from the bot account on Discord, so two
	 * IRC users reacting with the same emoji collapse into one. The reply
	 * tag is also read under its pre-ratification name, +draft/reply.
	 * Requires an IRCd with TAGMSG support (message-tags); reactions count
	 * against floodlines like lines do.
	 *
	 * Defaults to yes if not set.
	 *
	 * This directive is optional.
	 */
	#relayreactions = yes

	/*
	 * Which IRC channel events are told to Discord as an italic line from
	 * the bot account: any of join, part, quit, nick, topic, kick, comma
	 * separated. Services clients and pseudo clients never produce one.
	 *
	 * Defaults to "nick,topic,kick" if not set.
	 *
	 * This directive is optional.
	 */
	#relayevents = "nick,topic,kick"

	/*
	 * The name of the Discord webhook used for relaying IRC messages. An
	 * existing webhook with this name in a bridged channel is reused, which
	 * keeps the module from creating a new webhook on every restart. Discord
	 * limits the name to 80 characters. Changing it recreates the webhook of
	 * every bridge.
	 *
	 * Defaults to "IRC Bridge" if not set.
	 *
	 * This directive is optional.
	 */
	#bridgename = "IRC Bridge"

	/*
	 * The suffix appended to the nick of an IRC user when their message is
	 * relayed to Discord, so that IRC users can be told apart from Discord
	 * users with the same name.
	 *
	 * Defaults to " (IRC)" if not set.
	 *
	 * This directive is optional.
	 */
	#webhooksuffix = " (IRC)"

	/*
	 * Whether an IRC "@nick" which names one of a bridge's own pseudo
	 * clients is relayed as a real mention, so the remote user is pinged.
	 * Only the nicknames actually resolved can ping: a role mention, an
	 * @everyone, or a mention id typed by hand never does.
	 *
	 * Defaults to no if not set.
	 *
	 * This directive is optional.
	 */
	#relaymentions = yes
}

/*
 * The commands of BridgeServ. Bridges are network configuration, so every
 * command requires a permission; the defaults below give them to the same
 * operators who may jupe a server.
 */

/* Give it a help command. */
command { service = "BridgeServ"; name = "HELP"; command = "generic/help" }

/*
 * ADD <#channel> <space-id> <channel-id> [nick-suffix]
 *
 * Creates a bridge between an IRC channel and a channel on a bridged network.
 * The space (Discord guild) and channel are given by their ids, as shown by
 * GUILDS and CHANNELS. BridgeServ joins the IRC channel automatically. If a
 * nickname suffix is given then it is appended to the nick of every user
 * relayed into this channel, eg. iampaigeat/NC.
 */
command { service = "BridgeServ"; name = "ADD"; command = "bridgeserv/add"; permission = "bridgeserv/add" }

/*
 * SET <#channel> <space-id> <channel-id> [nick-suffix]
 *
 * Re-points an existing bridge at a different space and channel, and sets or
 * clears its nickname suffix. The webhook of the old channel is deleted.
 */
command { service = "BridgeServ"; name = "SET"; command = "bridgeserv/set"; permission = "bridgeserv/set" }

/*
 * DEL <#channel>
 *
 * Removes an existing bridge, deletes its webhook, and releases the nicknames
 * it had reserved on the network. Pseudo clients are removed from the channel
 * but remain on IRC for any other bridge they are active in.
 */
command { service = "BridgeServ"; name = "DEL"; command = "bridgeserv/del"; permission = "bridgeserv/del" }

/*
 * LIST
 *
 * Lists all configured bridges and whether the Discord link is up.
 */
command { service = "BridgeServ"; name = "LIST"; command = "bridgeserv/list"; permission = "bridgeserv/list" }

/*
 * STATUS [#channel]
 *
 * Shows, for every bridge or the one given, whether its network is connected,
 * how many pseudo clients it holds and how many are away, how much it has
 * relayed in each direction since the module loaded, how many lines the rate
 * limit refused, the state of its webhook, and when it last relayed anything.
 */
command { service = "BridgeServ"; name = "STATUS"; command = "bridgeserv/status"; permission = "bridgeserv/status" }

/*
 * GUILDS
 *
 * Lists the Discord servers (guilds) the bot is present in, as "name (id)".
 */
command { service = "BridgeServ"; name = "GUILDS"; command = "bridgeserv/guilds"; permission = "bridgeserv/guilds" }

/*
 * CHANNELS
 *
 * Lists the text channels of the given guild as "name (id)". The guild is
 * given by its id, as shown by GUILDS.
 */
command { service = "BridgeServ"; name = "CHANNELS"; command = "bridgeserv/channels"; permission = "bridgeserv/channels" }
